# relay-rpc
A handwritten RPC framework in Java, built to understand distributed systems fundamentals from first principles — custom serialization protocol, etcd-based service discovery and load balancing, and an OS-scheduling-inspired concurrent request scheduler (FCFS + MLFQ with aging).
